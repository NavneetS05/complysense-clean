// Use: User management screen for admins to add, edit, and deactivate users.

import { PageShell } from "../PageShell";
export default function Users() {
  return <PageShell title="User Lifecycle Manager" context="Institution user and role administration." />;
}
