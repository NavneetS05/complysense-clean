// Use: Expiry tracker showing calendar of contract and certification expiries.

import { PageShell } from "../PageShell";
export default function ExpiryTracker() {
  return <PageShell title="Expiry Tracker" context="Vendor contract and certification expiries." />;
}
