// Use: Conversational RAG Q&A chatbot interface for assessors.

import { PageShell } from "../PageShell";
export default function Chat() {
  return <PageShell title="Q&A Interface" context="Conversational RAG assessor interface." />;
}
