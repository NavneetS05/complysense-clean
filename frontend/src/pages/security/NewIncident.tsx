// Use: Incident form that registers title, category, and detection time.

import { PageShell } from "../PageShell";
export default function NewIncident() {
  return <PageShell title="Log Incident" context="CERT-In incident intake." />;
}
