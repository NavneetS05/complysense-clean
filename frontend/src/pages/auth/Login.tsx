// Use: Login page capturing user email and password credentials.

import { PageShell } from "../PageShell";

export default function Login() {
  return <PageShell title="Login" context="Authentication entry point for all roles." />;
}
