// Use: Form for submitting and resetting user password.

import { PageShell } from "../PageShell";

export default function ResetPassword() {
  return <PageShell title="Reset Password" context="Token-based password reset flow." />;
}
