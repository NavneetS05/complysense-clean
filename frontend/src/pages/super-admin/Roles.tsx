// Use: Read-only lookup of system RBAC permissions.

import { PageShell } from "../PageShell";
export default function Roles() {
  return <PageShell title="Role & Permission Viewer" context="Read-only RBAC matrix." />;
}
