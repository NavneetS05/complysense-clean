// Use: Detail view showing active users, departments, and logs for one institution.

import { PageShell } from "../PageShell";
export default function TenantDetail() {
  return <PageShell title="Institution Detail" context="Tenant users, departments, and audit logs." />;
}
