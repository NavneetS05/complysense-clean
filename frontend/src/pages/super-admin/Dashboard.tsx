// Use: Global dashboard depicting tenant status, health maps, and usage stats.

import { PageShell } from "../PageShell";
export default function Dashboard() {
  return <PageShell title="Global Health Map" context="Super Admin dashboard." />;
}
